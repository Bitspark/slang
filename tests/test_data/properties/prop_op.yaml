services:
  main:
    in:
      type: trigger
    out:
      type: number

properties:
  - val

operators:
  const:
    operator: slang.const
    generics:
      valueType:
        type: number
    properties:
      value: $val

connections:
  (:
    - (const
  const):
    - )
