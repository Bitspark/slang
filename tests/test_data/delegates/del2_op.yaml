services:
  main:
    in:
      type: generic
      generic: gen
    out:
      type: boolean

delegates:
  cmp:
    in:
      type: boolean
    out:
      type: map
      map:
        a:
          type: generic
          generic: gen
        b:
          type: generic
          generic: gen

properties:
  val:
    type: generic
    generic: gen

operators:
  del:
    operator: .del_op
    generics:
      gen:
        type: generic
        generic: gen
  const:
    operator: slang.const
    generics:
      valueType:
        type: generic
        generic: gen
    properties:
      value: $val

connections:
  (:
  - (del
  del.val):
  - (const
  const):
  - (del.val
  del.cmp):
  - .cmp)
  (.cmp:
  - (del.cmp
  del):
  - )
