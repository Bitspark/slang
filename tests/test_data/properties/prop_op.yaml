services:
  main:
    in:
      type: trigger
    out:
      type: number

properties:
  val:
    type: generic
    generic: valueType

operators:
  const:
    operator: slang.const
    generics:
      valueType:
        type: number
    properties:
      value: $val

connections:
  (:
    - (const
  const):
    - )
