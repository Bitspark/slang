services:
  main:
    in:
      type: trigger
    out:
      type: number

properties:
  val:
    type: generic
    generic: valueType

operators:
  const:
    operator: slang.data.Value
    generics:
      valueType:
        type: number
    properties:
      value: $val

connections:
  (:
    - (const
  const):
    - )
