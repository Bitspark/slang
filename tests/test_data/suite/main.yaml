# Main
---
in:
  type: map
  map:
    first:
      type: number
    second:
      type: number
    third:
      type: number
out:
  type: number
operators:
  dbl1:
    operator: ".duplicator"
    generics:
      itemType:
        type: number
  dbl2:
    operator: ".duplicator"
    generics:
      itemType:
        type: number
  dbl3:
    operator: ".duplicator"
    generics:
      itemType:
        type: number
  tl:
    operator: ".takers.takeLeft"
    generics:
      itemType:
        type: number
  tr:
    operator: ".takers.takeRight"
    generics:
      itemType:
        type: number
  p:
    operator: ".polynomial"
connections:
  <-first:
  - dbl1<-
  <-second:
  - dbl2<-
  <-third:
  - dbl3<-
  dbl1->:
  - tl<-
  dbl2->:
  - tr<-
  dbl3->left:
  - p<-a
  dbl3->right:
  - p<-b
  tl->:
  - p<-c
  tr->:
  - p<-x
  p->:
  - ->
