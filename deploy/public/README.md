# Previous playground operations moved

Deployment implementation, checks and the operational runbook now belong to the
private [slang-infra repository](https://github.com/Bitspark/slang-infra/tree/main/deploy/public).
Use that checkout for deployment commands. This file only preserves existing
documentation links. The engine repository owns Go source and binary releases.

The repository split did not change the running deployment or its recorded
release revisions. Earlier versions of this directory remain in Git history.
