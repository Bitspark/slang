services:
  main:
    in:
      type: trigger
    out:
      type: number

properties:
  val:
    type: generic
    generic: valueType

operators:
  const:
    operator: slang.data.Constant
    generics:
      valueType:
        type: number
    properties:
      value: $val

connections:
  (:
    - (const
  const):
    - )
